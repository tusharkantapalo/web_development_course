document.querySelector("button").classList.add("invisible");    //it adds the class invisible to the tag
document.querySelector("button").classList.remove("invisible"); //it removes the class from the tag
document.querySelector("button").classList.toggle("invisible"); //it toggles the class invisible
