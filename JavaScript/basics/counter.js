var exp = prompt("Enter your sentence: ");

/*  
var num = exp.length;
var left = num - 222;

alert("You have written " + num + " characters, you have " + left + " characters left.");//in this we can use with fewer variables by creating bracket
*/

alert("You have written " + (exp.length) + " characters, you have " + (exp.length - 222) + " characters left.");
